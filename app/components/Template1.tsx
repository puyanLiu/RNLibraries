import React from 'react';
import {
  View,
  Text,
  StyleSheet,
} from 'react-native';
import defaultStyles from 'rn-styles';

interface IProps {
}

const Template1 = (props: IProps) => {
  return (
    <View><Text>哈哈</Text></View>
  );
};

const styles = StyleSheet.create({
});

export default Template1;
